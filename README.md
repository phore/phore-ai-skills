# phore-ai-skills
## Install

```bash
git submodule add -b main git@github.com:phore/phore-ai-skills.git .pi/skills

## Laden / aktualisieren
git submodule update --init --recursive
```

Bei Änderungen am Submodule: Erst submodules commit / push - dann im Hauptprojekt commit / push.

